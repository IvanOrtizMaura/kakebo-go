# For local development with Firebase emulator only.
# Copy to .env and fill in your key.
# In production, the key is stored in Google Cloud Secret Manager:
#   firebase functions:secrets:set OPENAI_API_KEY
OPENAI_API_KEY=sk-...
